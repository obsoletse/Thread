第二章--对象及变量的并发访问
本章知识点:
	synchronized对象监听器为Object时的使用;
	synchronized对象监听器为class时的使用;
	非线程安全是如何产生的;
	关键字volatile的主要作用;
	关键字volatile与synchronized的区别;

2.1 synchronized 同步方法:
	什么是非线程安全? 非线程安全就是多个线程对同一个对象中的实例变量进行并发访问时发生，产生的后果是数据脏读。
	
	1.方法内的变量为线程安全:
	
		